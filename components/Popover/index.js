// Popover component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "Popover";
export const componentDescription = "The main popover component. The following properties can also be passed into another component\nthat has a property specifically for passing in popover properties.";
export const propsCount = 37;
export const requiredProps = ["bodyContent"];
