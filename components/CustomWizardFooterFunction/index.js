// CustomWizardFooterFunction component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CustomWizardFooterFunction";
export const componentDescription = "Callback for the Wizard's 'footer' property. Returns element which replaces the Wizard's default footer.";
export const propsCount = 0;
export const requiredProps = [];
