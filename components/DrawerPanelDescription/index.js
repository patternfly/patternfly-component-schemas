// DrawerPanelDescription component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "DrawerPanelDescription";
export const componentDescription = "Provides a description within the drawer panel. This should typically follow the drawer head.";
export const propsCount = 2;
export const requiredProps = ["children"];
