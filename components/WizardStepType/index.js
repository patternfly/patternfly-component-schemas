// WizardStepType component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "WizardStepType";
export const componentDescription = "Encompasses all step type variants that are internally controlled by the Wizard.";
export const propsCount = 0;
export const requiredProps = [];
