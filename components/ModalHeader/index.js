// ModalHeader component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "ModalHeader";
export const componentDescription = "Renders content in the header of the modal";
export const propsCount = 9;
export const requiredProps = [];
