// CustomWizardNavItemFunction component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CustomWizardNavItemFunction";
export const componentDescription = "Callback for the Wizard's 'navItem' property. Returns element which replaces the WizardStep's default navigation item.";
export const propsCount = 0;
export const requiredProps = [];
