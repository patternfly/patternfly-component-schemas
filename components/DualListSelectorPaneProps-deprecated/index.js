// DualListSelectorPaneProps-deprecated component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "DualListSelectorPaneProps-deprecated";
export const componentDescription = "Acts as the container for a list of options that are either available or chosen,\ndepending on the pane type (available or chosen). A search input and other actions,\nsuch as sorting, can also be passed into this sub-component.";
export const propsCount = 21;
export const requiredProps = [];
