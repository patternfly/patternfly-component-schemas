// CustomWizardNavFunction component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CustomWizardNavFunction";
export const componentDescription = "Callback for the Wizard's 'nav' property. Returns element which replaces the Wizard's default navigation.";
export const propsCount = 0;
export const requiredProps = [];
