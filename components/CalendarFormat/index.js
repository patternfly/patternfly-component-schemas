// CalendarFormat component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CalendarFormat";
export const componentDescription = "Additional properties that extend from and can be passed to the main component. These\nproperties allow customizing the calendar formatting and aria-labels.";
export const propsCount = 12;
export const requiredProps = [];
