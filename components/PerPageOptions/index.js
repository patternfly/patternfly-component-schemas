// PerPageOptions component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "PerPageOptions";
export const componentDescription = "Properties to customize the content and behavior of the pagination dropdown options. These\nproperties should be passed into the pagination component's perPageOptions property.";
export const propsCount = 2;
export const requiredProps = [];
