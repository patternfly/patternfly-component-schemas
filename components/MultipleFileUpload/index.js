// MultipleFileUpload component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "MultipleFileUpload";
export const componentDescription = "Acts as a container for all other MultipleFileUpload sub-components. This sub-component\nalso provides the functionality for file uploads, and access to the uploaded files via\na callback.";
export const propsCount = 5;
export const requiredProps = [];
