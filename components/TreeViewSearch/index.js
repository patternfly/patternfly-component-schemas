// TreeViewSearch component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "TreeViewSearch";
export const componentDescription = "Renders a search input for the tree view. This sub-component should be passed into the\ntree view component's toolbar property.";
export const propsCount = 5;
export const requiredProps = [];
